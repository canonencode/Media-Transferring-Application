# İnceleme Turu 3 — 22 Eylül 2026

Beş paralel ajan, beş açı: eşzamanlılık, test denetimi, rapor doğrulama, SQLite kuru koşusu,
taze gözle bug avı. Hedef: son inceleme turundan (a9a7f5e) **sonra** yazılan ve hiç denetlenmemiş
kod — bekçi/iptal/eskalasyon, `ScanOutcome`, cihaz kimliği, `PRE-SQLITE` etiketleri.

Cihaz bağlı olmadığı için gerçek cihaz doğrulaması bu turda yok.

**Özet:** 6 kritik bug (ikisi bağımsız olarak iki ajan tarafından bulundu), 6 doğruluk sorunu,
6 SQLite engeli, 15 rapor tutarsızlığı. Testler 198 → **291**, hepsi geçiyor, saf sınıflarda
**sıfır** üretim hatası bulundu.

---

## A. KRİTİK — push'tan önce düzeltilmeli

Hepsi `Program.cs`, hepsi bekçi/iptal kodunda. İki ajan bağımsız olarak aynı altısını buldu.

| # | Bug | Satır | Ne oluyor | Düzeltme |
|---|---|---|---|---|
| A1 | **Ctrl+C canlı çağrının ortasında COM bırakıyor** | 293, 687-706 | `CancelKeyPress` handler doğrudan `CloseSession()` çağırıyor; tarama thread'i o an `GetValues` içindeyse yakalanamaz access violation. Timeout yolunda düzelttiğim hatanın Ctrl+C'de yeniden yaratılmış hali. | Handler: `e.Cancel = true; Volatile.Write(ref scanAborted, 1); device.Cancel();` — kapatmayı ana thread'in `finally`'sine bırak. |
| A2 | **Retry pass bekçiye görünmez + iptal bayrağını görmüyor** | 485-579; `lastProgressTicks` yalnızca 767'de yazılıyor | Retry 1.400 nesneyi ~1,5 sn/nesne ile yeniden denerken 45 sn ilerleme sinyali yok → bekçi **sağlıklı cihazı** kilitlendi sanıp `FailFast`. Retry döngüsü bayrağı yalnızca girişte kontrol ediyor. | Retry döngüsünün her adımında `lastProgressTicks` yaz ve `scanAborted` kontrol et. |
| A3 | **`Completed` iptal edilmiş taramada `true`** | 461 | Bayrakla `break` normal dönüş sayılıyor, `IsCompletedSuccessfully` true. Konsol sink `Stalled`'a da baktığı için maskeliyor; **SQLite sink'i iptal edilmiş taramayı tam kaydeder** — `ScanOutcome`'un önlemek için var olduğu şey. | `Completed: scanTask.IsCompletedSuccessfully && Volatile.Read(ref scanAborted) == 0` |
| A4 | **Bekçi, serbest bırakılmış nesnede `Cancel()` çağırabilir** | 345-360 vs 454 | Hafif bir stall'da tarama ms içinde geri sarar, ana thread `CloseSession` ile `device`'ı bırakır; bekçi hâlâ `Cancel()`'a gidiyorsa `InvalidComObjectException` — `catch (COMException)` yakalamaz, thread'de yakalanmamış istisna = süreç çöker. | `Cancel()`'ı bayraktan önce çağır ve/veya `catch (Exception)`. |
| A5 | **30 dk ana timeout bayat** | 408-440 | `Cancel()` çağırıyor ama `scanAborted` set etmiyor (bekçinin öğrettiği "Cancel tek başına durdurmaz" dersi buraya taşınmamış), `OnScanFinished` da yok. 30 dk süren *sağlıklı* bir taramayı özet vermeden öldürür. | Ya sil (bekçi 65 sn'de hallediyor), ya bekçiyle aynı adımları uygula. |
| A6 | **İptal geri sarmasında seviye başına fazladan `Next()`** | 741-800 | Çocuk `PrintTree` döndükten sonra ebeveyn döngüsü bayrağı kontrol etmeden `childIds.Next()` çağırıyor. Kilitli cihazda her seviye bir bloke olabilecek çağrı daha. Sızıntı yok, ama "bayrak döngüyü durdurur" tasarımını deler. | Bayrağı `do` gövdesinin **başında**, `Next()`'ten önce kontrol et. |

## B. DOĞRULUK — verdi/sayaç yalan söylüyor

| # | Sorun | Satır | Düzeltme |
|---|---|---|---|
| B1 | **`SubtreeLosses` retry'ın kurtardığını saymıyor.** Klasör ana yürüyüşte listelenemez, retry başarıyla yürür, verdi yine "N klasör listelenemedi" der. Güvenli yön ama yalan. | 466, 391, 660 | Kurtarılan konteyner ID'lerini tut, çıkar. |
| B2 | **`filePropertyMisses` re-walk'ta çift sayıyor.** Retry bir klasörü yeniden yürüyünce zaten sınıflandırılmış çocuklar tekilleştirmeden *önce* `GetObjectInfo`'dan geçiyor. Aynı yol `newFailures`'ı ve `failedObjects`'ı da şişiriyor. | 571, 1094-1108 | `PrintTree`'de `GetObjectInfo`'dan önce `classifiedObjectIds` kontrolü. |
| B3 | Probe enumerator `Next` fırlatınca sızıyor | 530-537 | `ReleaseComObject(probe)` `finally`'ye. |
| B4 | Bekçinin `scanTask.Wait(20_000)`'i `AggregateException` fırlatabilir (tarama grace süresinde fault olursa) → bekçi thread'inde yakalanmamış, süreç `CloseSession`/`OnScanFinished` olmadan ölür | 372 | `try { done = Wait(...) } catch (AggregateException) { done = true; }` |
| B5 | `OnScanFinished` iki kez çağrılabilir (bekçi `Wait` false döndükten sonra, `FailFast`'ten önce tarama açılırsa — ms penceresi) + `sink.Errors.Count(...)` LINQ'su eşzamanlı `Add` ile `InvalidOperationException` | 385 vs 460 | `Interlocked.Exchange` guard, `sessionClosed` gibi. |
| B6 | `stallDetected` düz bool, `Volatile.Write`'tan *sonra* yazılıyor → formal happens-before yok. Pratikte x64'te görünür. | 345-346 | Satırları değiştir. |

## C. SQLite ENGELLERİ — ilk `INSERT`'ten önce

| # | Engel | Kanıt | Düzeltme |
|---|---|---|---|
| C1 | **Soyutlama kâğıt üzerinde.** `var sink = new ConsoleScanSink()` — `Program.cs` özet için `sink.MediaFiles`, `sink.Errors`, `sink.UndeterminedFiles` okuyor. `IScanSink`'e karşı **hiçbir şey derlenmiyor**. `SqliteScanSink` yazılsa `Program.cs` derlenmez. | 217, 385-393, 460-466, 602-674 | Yürüyüş kendi sayaçlarını tutsun (`subtreeLosses`, `undetermined`); `Program.cs` sink property'lerine dokunmasın. |
| C2 | **Cihaz kimliği sink'e ulaşmıyor.** Seri/model/üretici sadece yazdırılıyor; model ve üretici değişkende bile tutulmuyor. | 267-272 | `DeviceIdentity(DeviceId, FriendlyName, Serial?, Manufacturer?, Model?)` record'u; `OnScanStarted(DeviceIdentity, bool)`. |
| C3 | **İki sink aynı anda çalışamıyor.** SQLite'a geçince konsol kör olur. | — | 20 satırlık `CompositeScanSink`. |
| C4 | **`ScanOutcome` 1.400 okunamayan nesneli taramaya COMPLETE diyor.** `UnrecoveredObjects` ve `HiddenSubtrees` outcome'da yok. | 503, 535, ConsoleScanSink 87-88 | İki alan ekle; "temiz" = altı alanın hepsi. |
| C5 | **Dispose yolu yok.** `OnScanFinished` son garantili çağrı; `FailFast` finally çalıştırmaz. | 385, 395, 460 | `SqliteScanSink` commit'i `OnScanFinished` içinde senkron yapmalı. |
| C6 | **Depolama adı yerelleştirilmiş** ("Dahili depolama"). Telefonun dili değişirse her yol değişir → her dosya yeni, her eski kayıt silinmiş. | 909 | Depolama nesne ID'sini ipucu olarak taşı; `DeviceObject`'e `IsStorage` bayrağı. Android'in MTP depolama adını yeniden yerelleştirip yerelleştirmediği **ölçülmeli**. |

**`PRE-SQLITE` etiketlerinde 4 hata** (kendi yazdıklarım):
- `:237` `classifiedObjectIds` "gider" — **abartı**. Re-walk'ta tekrar imza I/O'sunu da engelliyor. Kalır.
- `:258` `filePropertyMisses` → hata satırı — "yokluk veri, hata değil" (`:1060`) ile çelişir. Sayaç gitsin, NULL sütundan türetilsin, satır yok.
- `:599` "ConsoleScanSink veriyi zaten tutuyor" — imza sayaçları ve `health.*` için **yanlış**, hiçbir sink'e ulaşmıyorlar.
- `:208` `ScanStage.Stream`'e atıf — **öyle bir değer yok**. Bool-return refactor'u doğru, ama enum'a değer eklemek de gerekir.
- Etiketsiz ama olması gereken: retry sayaçları/çıktıları (500-503, 584-595), devre kesici uyarısı (937), seri/model yazdırmaları (268-269).
- `:204` `classified_by` sütunu — `OnFile` "uzantıyla mı imzayla mı" bilgisini taşımıyor; parametre gerekir.

**Diğer SQLite notları:** `ModifiedRaw` byte-byte karşılaştır, parse-sonra-karşılaştır asla; `TryParseExact("yyyy/MM/dd:HH:mm:ss.fff", Invariant)`, UTC'ye çevirme (bölge bilinmiyor); saf `WpdDateTime` helper + tr-TR testi. `UNIQUE(device_id, path)` BINARY collation (Android case-sensitive). Tek `lock`, `OnScanFinished` idempotent + senkron commit + `Monitor.TryEnter(10s)`. DB `%LOCALAPPDATA%`'da, OneDrive klasöründe değil. `recovered` bayrağı satır bilgisi türetmek için gürültü; retry-düzeyi olay (`OnRetryPassFinished`) eksik.

## D. TEMİZLİK

| # | Ne | Satır |
|---|---|---|
| D1 | Bayat yorum: silinmiş `ShouldSkipFolder`'ı anlatıyor; `IsCameraMode`'un yorumu `ReadDeviceString`'in üstünde | 818-840 |
| D2 | Bayat yorum: "BEFORE the session cleanup below", `GetNameAndType` — ikisi de yok | 472-476 |
| D3 | Bayat adlar: `totalFilesSeen`, `scanErrors` | 1053, 1062 |
| D4 | `InvalidCastException` catch spekülatif — tip uyuşmazlığı COMException olarak gelir; `IsCameraMode`'da aynı catch yok, tutarsız | 1075, 877 |
| D5 | `DeviceObject`/`ScanError`/`ScanStage` `Program.cs`'in sonunda; test projesi COM yüzünden derleyemediği için **kopyasını** tutuyor (`ProgramDataTypes.cs`). `FileKind` gibi kendi dosyalarına taşınmalı, kopya silinmeli. | 1145-1164 |
| D6 | `FolderPolicy` sondaki boşluğu kırpmıyor (`".thumbnails "` atlanmaz). Test ajanı: savunulabilir — exact-name skip *açık* fail eder (gereksiz yürüyüş zaman kaybı; yanlış atlama fotoğraf kaybı). Karar gerekiyor: yorumda onayla ya da kırp. | FolderPolicy.cs |

## E. RAPOR TUTARSIZLIKLARI (`Proje-Raporu.pdf`)

**Yanlış:**
1. §15.9 "JPEG-XL ve JP2 uzantı listesindeydi" — hiç değillerdi. Sadece TIFF/DNG ve WebM/MKV listedeydi. Commit a9a7f5e mesajı da aynı hatayı taşıyor.
2. §8.1 başlangıç "17.416 medya, 3.136 hata" → commit kaydı **17.478 / 3.012**. %46 doğru.
3. §14 uzantı sayıları 27→**28**, 22→**23**; "300 karakter" → **3.000**.

**Çelişkili:**
4. "Program yalnızca IScanSink'e bağımlı" (Şekil 1, §5, §15.12) — yanlış, bkz. C1.
5. `ConsoleScanSink` "Saf mı? Evet" (§3) — raporun kendi tanımıyla çelişir, konsola yazar.
6. Test bulguları §7'de 4, §12-13'te 7 (doğrusu 7).
7. Kalıp sayısı §5/§13'te 8, §12'de 6.
8. "Beş inceleme turu" (§1) — git iki tur gösteriyor (§13 doğru).
9. §6.3 PTP "her iki modda doğrulandı" — tespit doğrulandı, `OnScanStarted → PARTIAL` zinciri **hiç tetiklenmedi** (§14 doğru).

**Bayat:**
10. §14 akış tablosu satırları: 9→217-218, 10→231, 16→292-293, 17→295-299, 20→402-440.
11. "6 parçaya bölündü" → 7 dosya (`FileKind.cs` sonradan).
12. §6.1 "ok bayrağı" → artık `DeviceObject?` null dönüş.
13. Şekil 1: `Undetermined` → `UndeterminedFiles`; `DeviceObject` alan sırası.
14. "12 imza" → 13 kontrol.
15. Test değişiklikleri commit edilince §14'teki "11 dosya, 2.945 satır, 198 test" bayatlar.

## F. TESTLER — 198 → 291

- **49 yeni `ConsoleScanSinkTests`** — daha önce sıfır testi vardı: girinti türetme (kök/iç içe/boş/çift slash/backslash), her `FileKind`'ın sayımı, `[UNCHECKED]` basılır `Unknown` basılmaz, verdi mantığı (altı bayrağın her biri tek başına PARTIAL + doğru sebep satırı), kamera uyarısı, uçtan uca ağaç render'ı.
- **+31 imza testi**: her imza 4096 byte'lık gürültülü tamponda; üç ses markası; ftyp kutu uzunluğu sınırları (4/8/24/26/1024/1028); BMP boyut sınırları (13/14/0x1FFFFFFF/0x20000000); ISOBMFF sarmalı JXL (hiç testi yoktu).
- **+5 ad testi**: `.trashed-`/`.pending-` büyük-küçük harf ve yol içinde; `.trashed.jpg` (tire yok) medya kalır.
- **+7 klasör testi**: çıkartma sebep metinleri; dört sebep birbirinden farklı; null açık fail; sondaki/çift ayraç Android kuralını bozmaz; **tr-TR kültürü** (her kuralın eşleşme dizesinde 'I' var).
- **+1 devre kesici**: sonuçtan önce atlanan kontroller pencereyi doldurmuyor.
- **Düzeltilen bayat test yorumları** (4): `EndsWith` atfı, "marka incelenmiyor" (inceleniyor), "byte 7" (artık 11), "BM en keskin durum" (boyut kontrolü sonrası anlamsız → `FF 0A` JXL ile değiştirildi).
- **Sonuç:** 291/291, 61 ms. **Saf sınıflarda üretim hatası bulunmadı** — her yeni test ilk koşuda geçti.
- Yan ürün: test projesi artık `IScanSink.cs` ve `ConsoleScanSink.cs`'i de linkliyor + `ProgramDataTypes.cs` (bkz. D5).

## G. TEMİZ ÇIKANLAR — bakıldı, sorun yok

- Bekçi ateşlenir ama tarama 20 sn içinde geri sarar → tek `OnScanFinished`, çift rapor yok
- `scanFaulted` yalnızca ana thread'de yazılıp okunuyor
- Ana thread `CloseSession`'ı yalnızca `scanTask` bittikten sonra çağırıyor — tarama boşta
- Retry yol yeniden kurma (`pathIsParent`) her `ScanStage` için doğru; `""` kök ve tek segment doğru
- Tekilleştirme: dosyalar 901'de tek noktada, konteynerler 785 (yürüyüş) ve 567 (retry) — iki yolda doğru
- `expected` her `TryRead`'den önce hesaplanıyor; konteyner hiç sayılmıyor
- 12 byte döngüsü: `filled` 12'yi aşamaz, pozitif `got` her zaman ilerler, sonsuz döngü yok
- `ReadDeviceString`/`IsCameraMode` COM nesnelerini her çıkışta bırakıyor
- `WPD_DEVICE_TYPE_CAMERA = 1` doğru
- Konsol satırları thread'ler arası bölünmüyor (`SyncTextWriter`, tek string)
- Kronometre kalıntısı yok; tüm üst düzey değişkenler kullanılıyor
- null/`Unknown` ayrımı testlerde iki yönlü ve kesin

## H. ÖNERİLEN SIRA

1. **A1-A6** (kritik, hepsi bekçi/iptal bölgesinde, tek commit)
2. **B1-B6** (aynı bölge, aynı commit'e girebilir)
3. **D1-D5** (temizlik, hızlı)
4. Cihaz takıldığında: 13466/164/13790 + `[NOTE]` yok + zorlamalı stall testi (A2 için retry sırasında) → **push**
5. Rapor düzeltmeleri E1-E15 (kod değişince satır numaraları yeniden alınır)
6. **C1-C6** SQLite hazırlığı — ayrı bir iş, ayrı plan

Toplam: A+B+D ≈ 2-3 saat, tek dosya. C ayrı gün.
